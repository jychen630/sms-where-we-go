version: '3.9'

services:
  nginx-prod:
    environment:
      - NGINX_HOST=demo.wherewego.cn

  web-prod:
    image: pig208/sms-where-we-go-web:demo
    build:
      context: ./frontend
      args:
        API_HOST: "http:\\/\\/demo.wherewego.cn\\/v1"
    environment:
      - DEMO=1

  api-prod:
    environment:
      - DEMO=1
